@model IEnumerable<TestREA.Models.ReaDroitGroupe>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Autorise)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdApplicationNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGroupeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Autorise)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdApplicationNavigation.Application)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdGroupeNavigation.Libelle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
