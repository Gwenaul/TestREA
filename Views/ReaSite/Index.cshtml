@model IEnumerable<TestREA.Models.ReaSite>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomSite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodeSite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodeDepartement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adresse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Archive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdDirectionNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomSite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodeSite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodeDepartement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adresse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Archive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDirectionNavigation.Libelle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
